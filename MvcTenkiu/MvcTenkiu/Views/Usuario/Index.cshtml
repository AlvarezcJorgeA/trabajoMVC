@model IEnumerable<MvcTenkiu.Models.USUARIO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.US_NOMBRE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.US_CONTRAS)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.US_ALIAS)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.US_FCREACION)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new {  id=item.US_ID  }) |
            @Html.ActionLink("Details", "Details", new { id=item.US_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.US_ID })
        </td>
    </tr>
}

</table>
